div(class="panel panel-default")
  table(id="tour_AssemblyInstructions" class="table")
    thead
      tr
        th Address
        th Label
        th Opcode
        th Operand
        th Hexcode
    tbody
      tr(ng-repeat="item in assembly" ng-class="{'info':$index == currentAddress}")
        td {{ item.address | output:16 | fixedLength:4 | uppercase }}
        td {{ item.label }}
        td {{ item.op }}
        td {{ item.operand }}
        td {{ item.hexcode | output:16 | fixedLength:4 | uppercase }}
